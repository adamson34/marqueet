## What

<!-- What does this change and why? Link issues with "Closes #123". -->

## How was it tested?

<!-- Tests added, screenshots (`--screenshot`), hardware it ran on. -->

## Checklist

- [ ] Targets `dev` (only release merges target `main`)
- [ ] `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace` pass
- [ ] No new npm / JavaScript dependencies
- [ ] README TODO list updated if this completes or adds roadmap items
